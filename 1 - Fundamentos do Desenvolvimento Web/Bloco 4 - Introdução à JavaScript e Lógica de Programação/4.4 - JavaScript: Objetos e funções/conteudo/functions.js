/* Item 1: */

// Adição:
function adicao (a, b) {
    let sum;
    sum = a + b;
    return sum
}

// Subtração (a - b)
function adicao (a, b) {
    let sub;
    sub = a - b;
    return sub
}

// Multiplicação (a * b)
function multiplicacao (a, b) {
    let mult;
    mult = a * b
    return mult
}

// Divisão (a / b)
function divisao (a, b) {
    let div;
    div = a / b
    return div
}

// Módulo (a % b)
function modulo (a, b) {
    let mod;
    mod = a % b
    return mod 
}